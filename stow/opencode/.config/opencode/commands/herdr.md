---
description: Control Herdr workspaces, tabs, panes, and coding agents through its CLI when running inside a Herdr-managed pane.
---

Load and follow the `herdr` skill.

Arguments from the command: `$ARGUMENTS`
